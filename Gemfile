source "https://rubygems.org"

# Matches the versions GitHub Pages uses to build your site, so a local
# preview looks exactly like production. GitHub builds automatically on push;
# this is only needed if you want to preview locally with `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.7"
